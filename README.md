# Description

My personal website 

# Deployment 
Install dependencies locally 
`bundle install`  

Run Locally 
`bundle exec jekyll serve --watch`    

Commit and Deploy
`git add .`
`git commit -m "message"`
`git push origin main`    